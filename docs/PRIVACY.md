# 隐私说明

## Tesla 账号连接（可选）

本地蓝牙钥匙不要求登录账号。用户主动选择“连接 Tesla 账号”后，系统会打开 Tesla 官方 OAuth 页面；应用与 `txx.app` 后端不会接触 Tesla 账号密码。

Tesla OAuth access token 和旋转 refresh token 仅在 `api.txx.app` 服务器端加密保存。App 只保存可撤销的后端会话令牌，并使用 iOS Keychain 的“仅限本设备、首次解锁后可用”保护等级。退出账号会删除本机会话并撤销服务器端会话。联网模式会向 Tesla Fleet API 请求用户授权的车辆列表、车辆状态和控制操作；本地蓝牙密钥不会上传。

## 本地蓝牙模式

用户可以不连接 Tesla 账号。此时车辆命令只通过手机与车辆之间的本地蓝牙发送，Fleet API 后端不会收到车辆控制请求。

## 保存在设备上的数据

- 每辆车独立的 P-256 私钥保存在 iOS Keychain，访问级别为 `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly`（首次解锁后，锁屏状态下仍可读取，用于响应拉门认证），不会通过 iCloud 同步或迁移到新设备。
- 已配对车辆标识、车型缓存、被动钥匙开关、可选 VIN，以及最近 20 条命令名称、时间与成功结果保存在本机。
- 本地诊断记录不包含 VIN、位置、车辆密钥或音乐搜索内容，可随 App 数据一并删除。
- 自动化场景、主页布局、提醒设置和车辆自定义名称按车辆保存在本机。
- 创建预约时读取车辆返回的位置并直接发回同一车辆，用于位置绑定预约；App 不将坐标保存到偏好或上传项目服务器。
- VIN 只用于建立完整本地车辆会话并校验当前 BLE 广播，不上传到本项目的服务器。

卸载前建议先在 App 内移除车辆，再到车机“控制 > 锁”删除对应手机钥匙。清除 Keychain 或更换设备后需要重新授权。

## 音乐封面联网请求

Tesla BLE 媒体协议不返回封面。播放媒体时，App 会使用车机返回的歌名和歌手：

1. 先通过固定的第三方网易云 Swift SDK 匿名搜索网易云目录；
2. 精确匹配失败时请求 Apple iTunes Search 公共目录；
3. 两者都失败则仅显示本地图标。

这些请求会向相应音乐服务披露搜索词、IP 地址及常规网络请求信息。App 不发送 VIN、车辆密钥或车辆控制数据。网易云接口属于第三方逆向实现，可能受网易风控或协议变化影响。

## 系统权限

- 蓝牙：扫描、配对、认证和控制车辆，以及 CoreBluetooth 后台状态恢复。
- Face ID／Touch ID／设备密码：确认前备箱和驾驶授权等敏感操作。
- 通知：在 App 获得真实车辆快照后提示低电量、门窗未关或充电异常；默认关闭。
- WatchConnectivity：在 Apple Watch 与配对 iPhone 之间传递控制请求及电量、续航和锁车状态快照。

App 不请求定位、通讯录、相册或麦克风权限。Tesla 账号授权仅在用户主动选择连接账号时发起。
